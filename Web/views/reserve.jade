extends layout

block content
  div.row.mt-3
    div.col
      div.card
        div.card-body
          h4 予約画面
          p LA TERRAの席の予約を行えます
          form(method="post").mt-5
            div.form-group
              label(for="seatType") 席の種別
              select.form-control(id="seatType")
                option(value,selected,disabled) 席の種別を選択してください
                option 時間無制限
                option 20分席
                option 15分席
            div.form-group
              label(for="seatType") 日付
              select.form-control(id="seatType")
                option(value,selected,disabled) 日付を選択してください
                option 今日
            div.form-group
              label(for="seatType") 時間帯
              select.form-control(id="seatType")
                option(value,selected,disabled) 時間帯を選択してください
                option 11:00~12:00
                option 12:00~13:00
                option 13:00~14:00
            input(type="submit",name="送信する",value="送信する").btn.btn-primary.w-100